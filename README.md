# Personal Finance Dashboard - Backend

[![Go Version](https://img.shields.io/badge/Go-1.21+-00ADD8?style=for-the-badge&logo=go)](https://go.dev/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-4169E1?style=for-the-badge&logo=postgresql)](https://www.postgresql.org/)
[![Redis](https://img.shields.io/badge/Redis-7-DC382D?style=for-the-badge&logo=redis)](https://redis.io/)

Высокопроизводительный бэкенд для персонального финансового дашборда.

## 🚀 Особенности

- **Чистая архитектура** (Clean Architecture) с разделением на слои
- **Аутентификация** через JWT токены (access + refresh)
- **Управление транзакциями** с ручным вводом и импортом из CSV/XLS
- **Гибридная категоризация** (rule-based + ML через gRPC)
- **Мультивалютность** с поддержкой конвертации
- **Аналитика** с кэшированием в Redis
- **API документация** через Swagger/OpenAPI

## 📁 Структура проекта

```
.
├── cmd/
│   ├── api/              # Точка входа API сервера
│   └── migrate/          # Утилита для миграций
├── internal/
│   ├── config/           # Конфигурация приложения
│   ├── domain/
│   │   ├── model/        # Бизнес-модели
│   │   ├── repository/   # Интерфейсы репозиториев
│   │   └── service/      # Бизнес-логика (интерфейсы)
│   ├── dto/              # Data Transfer Objects
│   ├── handlers/         # HTTP handlers
│   ├── middleware/       # HTTP middleware
│   ├── repository/       # Реализации репозиториев (pgx)
│   ├── service/          # Реализации сервисов
│   └── grpc_client/      # gRPC клиент для ML-сервиса
├── pkg/
│   ├── logger/           # Логирование
│   └── validator/        # Валидация
├── migrations/           # SQL миграции
├── scripts/              # Вспомогательные скрипты
└── docker-compose.yml    # Docker инфраструктура
```

## 🛠️ Технологический стек

| Компонент | Технология |
|-----------|------------|
| Язык | Go 1.21+ |
| Архитектура | Clean Architecture |
| HTTP роутер | chi v5 |
| БД | PostgreSQL 16 + pgx v5 |
| Миграции | golang-migrate/migrate |
| Кэш | Redis 7 |
| gRPC | google.golang.org/grpc |
| JWT | golang-jwt/jwt/v5 |
| Документация | Swagger/OpenAPI |

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd finace-dashboard
```

### 2. Запуск инфраструктуры

```bash
docker-compose up -d
```

### 3. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env при необходимости
```

### 4. Применение миграций

```bash
go run cmd/migrate/main.go up
```

### 5. Запуск сервера

```bash
go run cmd/api/main.go
```

Сервер запустится на `http://localhost:8080`

## 📚 API Документация

После запуска сервера Swagger документация доступна по адресу:

```
http://localhost:8080/swagger/index.html
```

## 📋 Основные эндпоинты

### Аутентификация
- `POST /api/v1/auth/register` - Регистрация нового пользователя
- `POST /api/v1/auth/login` - Вход в систему
- `POST /api/v1/auth/refresh` - Обновление токена
- `POST /api/v1/auth/logout` - Выход из системы

### Транзакции
- `GET /api/v1/transactions` - Получить список транзакций
- `POST /api/v1/transactions` - Создать транзакцию
- `GET /api/v1/transactions/:id` - Получить транзакцию
- `PUT /api/v1/transactions/:id` - Обновить транзакцию
- `DELETE /api/v1/transactions/:id` - Удалить транзакцию

### Аналитика
- `GET /api/v1/analytics/spending-by-category` - Траты по категориям
- `GET /api/v1/analytics/comparison` - Сравнение с предыдущим периодом

## 🧪 Тестирование

```bash
# Запуск всех тестов
go test ./...

# Запуск с покрытием
go test ./... -coverprofile=coverage.out
go tool cover -html=coverage.out
```

## 🔧 Конфигурация

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `SERVER_PORT` | Порт HTTP сервера | `:8080` |
| `DB_HOST` | Хост PostgreSQL | `localhost` |
| `DB_PORT` | Порт PostgreSQL | `5432` |
| `DB_NAME` | Имя базы данных | `finance_dashboard` |
| `REDIS_HOST` | Хост Redis | `localhost` |
| `REDIS_PORT` | Порт Redis | `6379` |
| `JWT_SECRET` | Секретный ключ JWT | - |
